services:
  nginx:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: prod
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
